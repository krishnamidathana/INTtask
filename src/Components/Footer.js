const Footer = () => {
  return (
    <div style={{
      height: '40px',
      backgroundColor: '#333',
      color: '#fff',
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'center',
    }}>
      Footer Section
    </div>
  );
};

export default Footer;
